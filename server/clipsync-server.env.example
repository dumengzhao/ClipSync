# ClipSync Relay Server 环境变量（由 systemd EnvironmentFile 读取）
# 复制本文件为 clipsync-server.env 并修改后使用

# 数据存储目录（含 networks.json / server.key / admin.json / logs/）
CLIPSYNC_DATA_DIR=/opt/clipsync-server/data

# ---- 日志（可选）----
# 日志同时输出到 stdout（systemd journal：journalctl -u clipsync-server）与
# 按天滚动的文件 <日志目录>/clipsync-server.log.YYYY-MM-DD（保留 14 天）。
# 日志目录默认 <CLIPSYNC_DATA_DIR>/logs，可用下面的变量改到别处（如 /var/log/clipsync）。
#CLIPSYNC_LOG_DIR=/var/log/clipsync
# 日志级别：trace / debug / info(默认) / warn / error。也兼容 RUST_LOG 的写法。
# 排查「定时拉取到底跑没跑」用 debug（会记录每次 tick 的跳过原因）。
#CLIPSYNC_LOG_LEVEL=info

# 管理后台账号：**没有环境变量可配**。
# 首次启动后访问 http://<服务器>:20070/admin ，页面会要求填入你在本地生成的
# Argon2id 口令哈希串（服务端只存哈希，明文口令不经过服务端、也不落任何文件）。
# 忘记口令时：删掉 <CLIPSYNC_DATA_DIR>/admin.json 并重启，即可重新初始化。

# 监听地址（中继服务端口，桌面端填 ip:20070 接入）
LISTEN=0.0.0.0:20070

# ---- 客户端更新托管（可选，见 UPDATE_MODULE_PLAN.md）----
# 更新文件根目录（latest.json 与 files/<platform>/），默认 <CLIPSYNC_DATA_DIR>/update
#UPDATE_DIR=/opt/clipsync-server/data/update

# 生成 latest.json 下载链接用的公开基址（生产强烈建议显式设置，如 https://sync.example.com）。
# 缺省时回退请求头 X-Forwarded-Proto/Origin，且仅接受 https；无法确定时 latest.json 返回 500。
#UPDATE_PUBLIC_BASE=https://sync.example.com

# 管理端单次上传总大小上限（MB），默认 200；需与 nginx client_max_body_size 对齐
#UPDATE_MAX_UPLOAD_MB=200

# 受信反向代理的出口 IP（逗号分隔）。仅当反代**不在本机**时需要设置：
# 来自这些地址（或环回）的请求才会采信 X-Real-IP / X-Forwarded-For，
# 管理登录退避才能按真实客户端 IP 分桶；不设置则所有经异机代理的登录
# 共享代理 IP 一个桶（任意人 5 次错码即可锁死管理员）。
# 例：1Panel/OpenResty 独立主机反代到本机时填那台机器的出口 IP
#TRUSTED_PROXIES=203.0.113.20

# ---- GitHub 同步（可选）----
# 填 owner/name 即启用「服务端从 GitHub 拉取更新包」（GitHub 与仓库名用 / 分隔）。
# 用途：客户端所在网络连不上 GitHub 时，服务端把 GitHub 上的安装包转存到自己这里，
# 客户端走原有更新入口就能更新 —— 客户端无需任何改动。
# **也可以直接在管理页「GITHUB SYNC」里填仓库名（页面配置优先于此变量，改完立即生效、不用重启）**；
# 两边都没填 = 功能整体关闭（管理页会显示未配置）。
# 定时同步默认关闭，配置后可在管理页手动触发或开启定时。
# 需要服务端能访问 github.com 与 objects.githubusercontent.com。
#GITHUB_REPO=dumengzhao/ClipSync

# 服务器直连不通 GitHub 时用的出网代理（**不需要重启也能改**：管理页
# 「客户端更新 → GitHub 同步 → 出网代理」里配，配完点「测试连接」即可验）。
# 这里写的是**环境变量形式**，优先级低于管理页配置；curl 会自动读这些变量。
# 支持 http:// https:// socks5:// socks5h://（socks5h = 域名交给代理解析，
# 服务器自己解不出 github.com 时用它）。
#HTTPS_PROXY=http://127.0.0.1:7890
