# ClipSync Server (规划中)

此目录预留给服务端模块，当前为空。

## 规划状态

根据 [开发方案](../docs/development-plan.md)，ClipSync 采用纯 P2P 直连架构，**不需要中继服务器**。此目录用于未来可能增加的辅助服务端模块，例如：

- 设备发现引导服务（帮助跨网设备首次发现对端）
- 更新源镜像（自建 Tauri updater endpoint）
- 同步统计聚合服务（可选）

## 何时启动

待终端应用（`../client/`）完成 MVP 后再评估是否需要服务端。当前阶段不开发。
