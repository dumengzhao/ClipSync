# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
dist/
dist-ssr/
build/
target/
**/src-tauri/target/
**/src-tauri/gen/

# Generated app icon source (tools/gen-app-icon.py regenerates each run)
client/app-icon.png

# Tauri
**/WixTools/
**/.cargo/

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Environment
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Crash dumps
crash_dumps/

# Windows reserved device-name artifact (created by a `2>nul` misuse; cannot be deleted normally, so ignore it)
nul

# Local key material (never commit)
*.p12
*.pfx
*.key
*.pem
.tauri/

# Cargo lock is committed for binary, ignored for library
# This is a binary app, so Cargo.lock IS committed

# Agent workspace memory (local only, never commit)
.workbuddy/
