# CLAUDE.md

本文件由 Claude Code 自动读取。详细内容见 @AGENTS.md。

简要：
- ClipSync = Tauri v2 + Rust 跨平台剪贴板同步工具，纯 P2P，端到端加密
- 当前阶段：阶段一 MVP 早期，骨架已就绪，核心同步未实现
- 终端应用在 `client/`，服务端 `server/` 规划中
- 完整设计方案、已实现功能清单、开发要求、待办见 @AGENTS.md
- 开发：`cd client && npm run tauri dev`
- 提交前：`scripts/local-ci.sh`
